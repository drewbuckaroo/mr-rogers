function mrRogers(count) {
  let numList = ["1", "2", "3"];
  let roboList = ["Beep!", "Boop!", "Won't you be my neighbor?"];
  let numList = roboList;
  for (let i = 0; i < numList.length; i++){
  while (count >= robolist[i]) 
    result.push (numList[i]);
    count === roboList[i];
  }
}


$(document).ready(function(){
    $("#").submit(function(event) {
        event.preventDefault();

    });
});
